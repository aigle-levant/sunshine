PORT=
ANTHROPIC_API_KEY=
APIFY_TOKEN=

# Content Studio image generation. Without this the studio still writes copy —
# only the picture is skipped. Note that nodemon does not watch .env, so restart
# the backend after adding it.
GEMINI_API_KEY=

# Optional: pins the image model, tried before the built-in candidates.
GEMINI_IMAGE_MODEL=
